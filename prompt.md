При переходе на /admin (не /admin/geocode-keys, а именно корень /admin) 
выдаёт 404 — значит app/admin/page.tsx отсутствует или потерялся при 
одном из мерджей.

Проверь структуру app/admin/ — если page.tsx там нет, создай его:
простая стартовая страница "Меню разработчика" со списком карточек 
доступных инструментов (пока одна карточка — "Ключи геокодера" со 
ссылкой на /admin/geocode-keys), в стиле остального проекта.

Убедись что app/admin/layout.tsx (проверка что пользователь есть в 
таблице admins) существует и оборачивает эту страницу тоже, не только 
geocode-keys.